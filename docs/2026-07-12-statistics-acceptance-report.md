# 生产深度验收报告

验收日期：2026-07-12 至 2026-07-13
时区：Asia/Shanghai
环境：生产 `audit.sjiaa.cc.cd` 与 `api.sjiaa.cc.cd`

## 1. 验收范围

- 审计后台 12 个业务路由：首页、Sub2API 用户、模型消耗统计、用户充值、额度调整记录、历史账、赠送额度、经营账、对账中心、异常中心、操作审计、管理员账号。
- Sub2API 管理端：仪表盘、用户管理、兑换码、使用记录。
- 核对源：浏览器页面、Sub2API 官方统计、生产 PostgreSQL、生产审计 SQLite、私有附件文件。
- 交互覆盖：登录、创建管理员、筛选、重置、分页、列显隐、详情、调额、对账、经营支出、附件上传和下载。

## 2. 真实写入验收

### 管理员账号

- 通过生产页面创建“浏览器验收管理员”。
- 使用新账号真实退出、登录并访问管理员页面成功。
- 验收后停用测试账号并撤销 Token。
- 当前管理员：3 个，启用 2 个，停用 1 个。

### 额度调整

对 `test3@qq.com` 执行两笔最小金额调额：

- `ADJ202607120001`：调增 0.01，远端事件 56。
- `ADJ202607120002`：调减 0.01，远端事件 57。
- 余额变化：13.00 -> 13.01 -> 13.00。
- 两笔均使用“异常修正”，未生成虚假现金账或赠送账。

### 经营账与附件

- 新增经营支出 `EXP20260712164122WAXL`，金额 0.01，分类“生产验收”。
- Chrome 页面上传 `hero.png`，内置浏览器核验附件列表并点击下载。
- 附件 ID 3，大小 13,057 字节，类型 `image/png`。
- 本地文件与服务器私有文件 SHA-256 均为 `881ffbcaafc212e49addad08846a5b82761355fa20624253af3477ba33262c5c`。
- 下载请求真实到达 `/api/v1/attachments/3/download`，操作审计详情中的文件名、类型、大小和关联经营支出均正确。

内置浏览器不支持设置原生文件选择器，因此上传动作使用用户明确授权的 Chrome；附件列表、下载和审计验收仍在内置浏览器完成。

## 3. 当前账务汇总

| 项目 | 当前生产结果 |
|---|---:|
| 管理员 | 3 个，启用 2，停用 1 |
| 成功调额 | 8 笔 |
| 调增 / 调减 / 净额 | 712.01 / 0.01 / 712.00 |
| 现金账 | 3 笔，590.00 |
| 赠送额度 | 5 笔，122.00 |
| 现金 + 赠送 | 712.00 |
| 经营支出 | 1 笔，0.01 |
| 对账批次 / 问题 | 2 / 0 |
| 附件 | 1 个，13,057 字节 |
| 操作审计 | 13 条，其中高风险 12 条 |

账务恒等式成立：成功调额净额 `712.00 = 现金 590.00 + 赠送 122.00`。

## 4. 对账结果

- 2026-07-11：本地成功 5 笔、512.00；远端匹配 5 笔、512.00；问题 0。
- 2026-07-12：本地成功 2 笔、净额 0.00；远端匹配 2 笔、净额 0.00；外部事件 0；审计孤儿 0；问题 0。
- 两笔最小金额调额在 Sub2API 兑换码页面均显示为“余额（管理员）/ 已使用”。

## 5. 官方统计固定区间

### 2026-07-11

Sub2API 使用记录与审计后台模型统计完全一致：

- 请求数：2,237。
- Token：137,511,749。
- 实际消费：122.3536627。
- 模型数：7。
- 各模型行满足 `输入 + 输出 + 缓存创建 + 缓存读取 = 总 Token`。

### 2026-07-01 至 2026-07-09

- 请求数：8,506。
- Token：969,369,193。
- 实际消费：965.5262577。

包含当天的区间会持续写入，因此跨页面等值验收使用已经结束的自然日。

## 6. 字段语义核对

- Sub2API `users.total_recharged` 字段合计：907.00。
- 启用普通用户：11 个，当前余额合计 2,987.0100。
- `test3@qq.com` 当前余额 13.00，数据库 `total_recharged` 字段为 399.00，实际可追溯正向余额事件不是 399.00。
- 页面已明确显示“Sub2API 累计充值字段”，不再将该字段命名为本地现金实收。
- 首页“实收入账”使用切账后的本地现金账，和 Sub2API 字段分开显示。

## 7. 代码与部署验证

- 后端完整测试：63 个测试、490 个断言全部通过。
- 前端 `vue-tsc` 与 Vite 生产构建通过。
- 原 2,520 行全局样式已拆为 6 个模块，业务源码最大文件低于 1,000 行。
- 12 个页面使用路由动态导入，主入口 JS 由约 3.09 MB 降至约 335 KB。
- 生产健康接口返回 `status=ok`，时区为 `Asia/Shanghai`。
- 内置浏览器逐路由回归全部通过；`reconcile.run` 已显示为“运行 / 重跑对账”。

## 8. 验收过程说明

原生文件选择器首次回退测试时误选了两张非目标图片。发现后立即按精确附件 ID 清理数据库记录、私有文件和对应测试审计记录，并保留清理前备份。最终生产附件表只有本报告记录的 `hero.png`，不存在误传文件残留。

生产仍没有支付订单样本；历史账对支付订单来源的展示由自动化测试覆盖。异常中心当前只有作废单，没有二次确认异常样本。
